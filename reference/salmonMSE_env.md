# Environment to store salmon specific variables, e.g, fitness, during the simulation

Environment to store salmon specific variables, e.g, fitness, during the
simulation

## Usage

``` r
salmonMSE_env
```

## Format

An object of class `environment` of length 5.
